-- Historial remoto ya contiene la version 20260622090000.
--
-- Esta migracion puente mantiene el repositorio local alineado con la tabla
-- supabase_migrations.schema_migrations del proyecto remoto. El SQL original
-- de esa version fue aplicado fuera de este workspace antes de agregar las
-- reservas consumibles de Terraza, por eso no debe ejecutar cambios nuevos.
SELECT 1 WHERE false;
